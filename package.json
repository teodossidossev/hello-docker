{
  "name": "web-server",
  "version": "1.0.0",
  "description": "Simple Dockerized Node.js web server",
  "main": "index.js",
  "scripts": {
    "start": "http-server public -p 8080",
    "cy:open": "cypress open",
    "cy:run": "cypress run",
    "cy:test": "http-server public -p 8080 & cypress run"
  },
  "devDependencies": {
    "cypress": "^14.3.3",
    "http-server": "^14.1.1"
  }
}
